apiVersion: v1
kind: ConfigMap
metadata:
  name: notification-service-config
  namespace: vk-paints
data:
  RABBITMQ_URL: "amqp://rabbitmq"
  SMTP_HOST: "smtp.gmail.com"
  SMTP_PORT: "587"
  SMTP_SECURE: "false"
