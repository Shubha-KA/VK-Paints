apiVersion: apps/v1
kind: Deployment
metadata:
  name: retailer-service
spec:
  selector:
    matchLabels:
      app: retailer-service
  template:
    metadata:
      labels:
        app: retailer-service
    spec:
      containers:
      - name: retailer-service
        image: retailer-service:latest
        imagePullPolicy: Never
        env:
        - name: DB_URL
          value: postgres://postgres:postgres@postgres:5432/retailer_db
        - name: RABBITMQ_URL
          value: amqp://rabbitmq
        ports:
        - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: retailer-service
spec:
  selector:
    app: retailer-service
  ports:
  - port: 80
    targetPort: 3000
